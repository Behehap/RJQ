server:
  port: 8080

queue:
  workers: 5
  demo_delay_seconds: 10
  cooldown_seconds: 0

email:
  smtp_host: "smtp.gmail.com"
  smtp_port: 587
  smtp_user: ""
  smtp_pass: ""

retry:
  max_attempts: 3
  backoff_seconds: 5

timeout:
  job_seconds: 30

rate_limit:
  emails_per_minute: 3
  burst: 1
